# Eval fixture for pallets/flask. commit_sha records what the numbers in docs/EVALS.md were
# validated against — see docs/PLAN.md decision #17 for why it isn't an enforced checkout pin in v1.
repo_url: https://github.com/pallets/flask
commit_sha: d73fa1cdcbd8b1465c151db8924ba58b1dd14e35

questions:
  - question: "Where do I add a new route or endpoint in this codebase?"
    answerable: true
    expected_relevant_files:
      - src/flask/app.py
      - src/flask/sansio/scaffold.py
      - src/flask/sansio/app.py

  - question: "How does Flask sign and manage session cookies?"
    answerable: true
    expected_relevant_files:
      - src/flask/sessions.py

  - question: "How do Blueprints work for organizing routes in Flask?"
    answerable: true
    expected_relevant_files:
      - src/flask/blueprints.py
      - src/flask/sansio/blueprints.py

  - question: "How does the `flask run` command-line interface work?"
    answerable: true
    expected_relevant_files:
      - src/flask/cli.py

  - question: "What's the test setup for this repo -- what testing tooling and fixtures does it use?"
    answerable: true
    expected_relevant_files:
      - tests/conftest.py

  - question: "How does Flask manage the application/request context stack, and what's the difference between an app context and a request context?"
    answerable: true
    expected_relevant_files:
      - src/flask/ctx.py
      - src/flask/globals.py
    notes: "In Flask 3.2+, AppContext and RequestContext were merged into one AppContext class; request_ctx is now a deprecated alias."

  - question: "How does Flask wire up Jinja and render templates -- what happens when I call render_template()?"
    answerable: true
    expected_relevant_files:
      - src/flask/templating.py

  - question: "How does Flask turn exceptions into HTTP error responses, and where do I register a custom error handler?"
    answerable: true
    expected_relevant_files:
      - src/flask/app.py
      - src/flask/sansio/app.py

  - question: "Where does the `g` object come from and how does Flask load app configuration (e.g. from a file or environment)?"
    answerable: true
    expected_relevant_files:
      - src/flask/ctx.py
      - src/flask/globals.py
      - src/flask/config.py

  - question: "How does this codebase implement a GraphQL API?"
    answerable: false

  - question: "How does this repo manage Kubernetes deployments?"
    answerable: false
